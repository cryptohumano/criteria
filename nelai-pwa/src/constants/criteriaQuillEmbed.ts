/** Clase DOM del salto de página Quill; el PDF y el CSS deben usar el mismo valor. */
export const CRITERIA_QUILL_PAGE_BREAK_CLASS = 'criteria-quill-page-break'

/** Legacy: mantener por compatibilidad (Nelai). */
export const NELAI_QUILL_PAGE_BREAK_CLASS = 'nelai-quill-page-break'
