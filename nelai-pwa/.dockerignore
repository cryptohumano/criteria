# Artefactos locales — siempre se reconstruyen dentro del builder.
node_modules
dist
.yarn/cache
.yarn/install-state.gz
.yarn/unplugged
.yarn/build-state.yml

# Secretos y configuración local.
.env
.env.*
!.env.example
.certs

# El servicio Etherpad tiene su propio Dockerfile; no debe entrar a la imagen del API.
etherpad

# Misc
.git
.gitignore
.github
.vscode
.idea
.cursor
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tests / coverage / sourcemaps
coverage
.nyc_output
*.map

# Dockerfiles y scripts de despliegue que el contenedor no necesita.
Dockerfile
.dockerignore
docker-compose*.yml
docker-compose*.yaml
